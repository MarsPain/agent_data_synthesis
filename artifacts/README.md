# Runtime Artifacts

This directory is for local pipeline outputs such as generated samples, rejection logs, manifests, and executable fixture state.

Runtime outputs are ignored by git by default. Keep design documents, plans, and durable architecture notes under `docs/`.
